# Legacy Prototype

This directory preserves the original static React/Babel mock that the new monorepo is based on.

- `index.html`: original entrypoint
- `src/`: mock page components
- `styles/`: original stylesheet

It is retained as a design and content reference while the production-ready workspace is implemented in `apps/`, `services/`, and `packages/`.
