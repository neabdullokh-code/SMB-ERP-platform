# Kubernetes Placeholder

Kubernetes manifests are intentionally deferred until the runtime target is chosen.

Recommended contents when the deployment model is finalized:

- `namespace.yaml`
- `platform-api-deployment.yaml`
- `worker-deployment.yaml`
- `company-portal-deployment.yaml`
- `bank-portal-deployment.yaml`
- `ingress.yaml`

